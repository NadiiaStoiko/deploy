import { RegionalNumberFormatPipe } from './regional-number-format.pipe';

describe('RegionalNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RegionalNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
